namespace InterviewPracticing.Enumerators
{
    public enum MenuOption
    {
        DesignPatters = 1, WhatsTheDifference
    }
    public enum DesignPatternEnum
    {
        Singleton = 1, AbstractFactory, Facade, Bridge, Strategy, Observer
    }
}
